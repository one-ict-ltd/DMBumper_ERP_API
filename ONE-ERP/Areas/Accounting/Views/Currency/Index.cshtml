@{
    ViewData["Title"] = "Currency";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-body">
        <h5 class="card-title" style="color: black">Currency</h5>
        <hr>
        <form id="fromGroup" method="post" data-parsley-validate="parsley">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="row">
                <input type="hidden" id="currencyId" name="currencyId" value=0>
                <div class="col-sm-6">
                    <div class="form-group row">
                        <label for="currencyName" class="col-sm-4 col-form-label">Currency Name <span style="color:red;">*</span></label>
                        <div class="col-sm-8">
                            <input type="text" class="form-control" id="currencyName" name="currencyName" data-parsley-required="true">
                        </div>
                    </div>
                    <div class="form-group row">
                        <label for="aliasName" class="col-sm-4 col-form-label">Short Name <span style="color:red;">*</span></label>
                        <div class="col-sm-8">
                            <input type="text" class="form-control" id="aliasName" name="aliasName" data-parsley-required="true">
                        </div>
                    </div>
                </div>
                <div class="col-sm-6">
                    <div class="form-group row">
                        <label for="conversionRate" class="col-sm-4 col-form-label">Conversion Rate</label>
                        <div class="col-sm-8">
                            <input type="text" class="form-control" id="conversionRate" name="conversionRate" data-parsley-required="true">
                        </div>
                    </div>
                    <div class="form-group row">
                        <label for="isActive" class="col-sm-4 col-form-label">Status</label>
                        <div class="col-sm-8">
                            <select name="isActive" id="isActive" class="form-control">
                                <option value="">Select</option>
                                <option value="1">Active</option>
                                <option value="0">Inactive</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group row">
                        <label for="groupName" class="col-sm-4 col-form-label"></label>
                        <div class="col-sm-8">
                            <button type="button" id="btnSave" onclick="SaveAccountGroup()" class="btn btn-success btn-sm" style="width:80px;">Save</button>
                        </div>
                    </div>
                </div>
            </div>
            <hr>
        </form>
    </div>
</div>
<br />
<hr />
<div class="row clearfix">
    <div class="col-12">
        <div class="card mb-4">
            <!-- Card Header - Dropdown -->
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h6 class="m-0 font-weight-bold text-primary">Currency List</h6>
            </div>
            <!-- Card Body -->
            <div class="card-body">
                <table class="table table-striped table-bordered" id="groupTable">
                    <thead>
                        <tr>
                            <th>Sl#</th>
                            <th>Currency Name</th>
                            <th>Short Name</th>
                            <th>Conversion Rate</th>
                            <th>Status</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script>
        $(document).ready(function () {

            $('#groupTable').DataTable();
            
            Common.Ajax('GET', '/api/Currency/GetCurrency/', [], 'json', GetCurrency);

        });
        

        function GetCurrency(response) {
            if (response == null) {
                return false;
            }
            $("#groupTable").DataTable().destroy();
            var table = $("#groupTable tbody");
            table.empty();
            var index = 0;
            for (var i = 0; i < response.length; i++) {
                index = i + 1;
                table.append('<tr><td>' + index + '</td>'
                    + '<td>' + response[i].currencyName + '</td>'
                    + '<td>' + response[i].aliasName + '</td>'
                    + '<td>' + response[i].conversionRate + '</td>'
                    + '<td>' + response[i].status + '</td>'
                    + '<td><a class="btn btn-success" title="Edit" onclick="Edit(' + index + ')" ><i class="fa fa-edit"></i></a>&nbsp;<a href="javascript:void(0)"style="background-color:red;border-color:red" data-toggle="tooltip" title="Delete" class="btn btn-danger btn-xs" onclick="Delete(' + index + ')"><i class="fa fa-trash"></i></a></td>'
                    + '</tr >');
            }
            $("#groupTable").DataTable({
                stateSave: false,
                dom: "<'row'<'col-sm-12 col-md-2'l><'col-sm-12 col-md-3'B><'col-sm-12 col-md-7'f>>" +
                    "<'row'<'col-sm-12't>>" +
                    "<'row'<'col-sm-12 col-md-6'i><'col-sm-12 col-md-6'pr>>",
                buttons: [
                    {
                        extend: 'excelHtml5',
                        text: '<i class="fa fa-file-excel"></i> Excel',
                        titleAttr: 'Excel',
                        exportOptions: {
                            columns: [0, 1, 2, 3, 4]
                        },
                        title: "Currency",
                        class: 'btn btn-info'
                    },
                    {
                        extend: 'csvHtml5',
                        text: '<i class="fa fa-file-excel"></i> CSV',
                        titleAttr: 'CSV',
                        exportOptions: {
                            columns: [0, 1, 2, 3, 4]
                        },
                        title: "Currency",
                    }
                ]
            });
        }


    </script>
}


